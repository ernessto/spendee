import { css } from 'styled-components';

export default {
    wrapper: css`
        margin: 0 auto;
        position: relative;
        z-index: 150;
        padding: 15px;
        background-color: #fff;
        border-radius: 8px;
        margin-top: 20px;
        
    `
}